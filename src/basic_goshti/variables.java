package basic_goshti;

public class variables {
    static void main() {
        //int x = 5;
        //int y = 2;
        // or or
        //int x = 5; int y = 2;
        //if u run the programme it shows that 5/2 is 2 which is wrong as
        //as we have teken x and y as integers so int/int will give integer i.e integral part only
        // and not decimal one i.e so 5/2 is 2.5 it will oly give 2 will display 2 only
        //you can try remove // fromm int declared values and add // to values declared as double
        // write **double** double is nothing but **float** in java (for decimals)
        double x = 5 ;
        double y = 2 ;
        System.out.println(x+y);
        System.out.println(x-y);
        System.out.println(x*y);
        System.out.println(x/y);

    }
}
